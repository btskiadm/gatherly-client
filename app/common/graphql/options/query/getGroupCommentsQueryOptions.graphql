query GetGroupComments($groupId: String!) {
  getGroupComments(groupId: $groupId) {
    comments {
      id
      rate
      content
      createdAt
      rate
      user {
        createdAt
        email
        id
        largePhoto
        mediumPhoto
        role
        smallPhoto
        status
        username
      }
    }
  }
}
