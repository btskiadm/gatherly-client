query GetUsersByUsername($username: String!) {
  getUsersByUsername(username: $username) {
    id
    username
    email
    createdAt
    role
    status
    smallPhoto
    mediumPhoto
    largePhoto
  }
}
